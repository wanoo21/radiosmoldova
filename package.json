{
  "name": "radio-online",
  "version": "1.7.2",
  "description": "Asculta online toate posturile Radio din republica Moldova.",
  "main": "src/sw/background.js",
  "scripts": {
    "build:dist": "node scripts/build-dist.mjs",
    "build:zip": "npm run build:dist && VERSION=$(node -e \"console.log(JSON.parse(require('fs').readFileSync('manifest.json','utf8')).version)\") && (cd dist && zip -r \"../radio-mru-v${VERSION}.zip\" .)",
    "dev": "npm run build:dist",
    "stations:refresh": "node scripts/refresh-stations.mjs",
    "stations:refresh:apply": "node scripts/refresh-stations.mjs --apply",
    "stations:add": "node scripts/refresh-stations.mjs --add-new",
    "stations:add:apply": "node scripts/refresh-stations.mjs --add-new --apply",
    "test": "echo \"Error: no test specified\" && exit 1",
    "gulp": "echo \"Legacy gulp build removed in MV3 scaffold\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wanoo21/radiosmoldova.git"
  },
  "keywords": [],
  "author": "Prodan Ion",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wanoo21/radiosmoldova/issues"
  },
  "homepage": "https://github.com/wanoo21/radiosmoldova#readme",
  "devDependencies": {},
  "dependencies": {}
}
